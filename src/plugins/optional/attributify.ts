/**
 * Attributify Plugin (Stub)
 *
 * Attributify mode utilities for CoralCSS.
 * @module plugins/optional/attributify
 */

import type { Plugin } from '../../types'

export function attributifyPlugin(): Plugin {
  return {
    name: 'attributify',
    version: '1.0.0',
    install() {
      // Stub - attributify plugin not yet implemented
    },
  }
}

export default attributifyPlugin
